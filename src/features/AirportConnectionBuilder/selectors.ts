import { AirportConnectionBuilderState } from '../../types';
/**
 * @author Vishal Chavan
 */
export const getAirportConnectionBuilderState = (state: AirportConnectionBuilderState) => state ? state.airportConnectionBuilder : null;
