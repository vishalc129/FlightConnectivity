// import { AppErrorData } from "../models";

export const AppService = {
    // handleAPIError: (errorData: AppErrorData): void => {
    //     if (errorData.errorCode === 400 || errorData.errorCode === 401) {
    //         // AuthService.logoutUser();
    //     }
    // },

    // filterSearchResults: (data: { inputValue: string; item: { [x: string]: string } }, searchKey: string): boolean => {
    //     if (!data.inputValue || data.inputValue === '') {
    //         return true;
    //     }
    //     const searchInput = data.inputValue.trim().toUpperCase()
    //     if (data.item[searchKey].toUpperCase().includes(searchInput)) {
    //         return true
    //     }
    //     return false
    // }
}
